# 接班工作质量复核与下一轮任务

审核日期：2026-09-15。对象：基线 3cd16ba 上当前未提交工作区，重点复核木剑、地图旗标、向日葵收尾。不是对全书或全部历史代码的重新验收。

## 结论

三项功能基本成立；本次独立补测后没有发现阻断使用的故障。交付尚需收尾：府邸提示不一致，原专项存在不能证明其声明的测试，交接文件状态相互矛盾，未提交/打包/发布。原“全部收尾完成，可提交”的声明过宽。

## 发现（按影响排序）

1. **P2：府邸教学提示遗漏木剑。** scripts/mansion.gd:40 路牌、357 准备目标、360 战斗目标，以及 scripts/hud.gd 的森林完成后欢迎语仍只提示石斧。实际准入和说明支持木剑或石斧。初学者持木剑时会被要求另做石斧。统一四处文案，检查实际画面是否溢出。
2. **P2：原专项不能证明两项验收结论。** tests/test_book_review.gd:95–103 声称“仅木剑”，但 fixture 有 4 把石斧且未移除；153–164 直接调用 toggle_waypoint，未经过鼠标/GUI；键盘 5 的原断言也从已装备木剑开始。功能本次补测通过，但原测试仍应修正，防止以后发生回退而测试继续报通过。
3. **P2：交接状态不一致。** HANDOVER 宣称已完成第 1–8 步，但仍保留“A 测试待创建”、已完成验证与待打包发布混杂的措辞；“等待用户确认才提交”也不是本轮新增的用户要求。保留历史记录并另列唯一的当前状态，不让下一人重复工作或跳过遗漏。
4. **P3：视觉证据仍只支持基础识别。** 本次重新看书第 9、25 页和三张已有游戏截图。木剑棕色阶梯轮廓、向日葵高茎/金色花面等主要特征可辨；仍是简化程序模型，没有逐尺寸/纹理细节验收。不得把这次结果升级为像素级还原或整页完成。

## 独立验证

- 复跑 test_book_review：headless 0 failures。
- 新增 tests/test_quality_review.gd：headless 和原生 Metal / Forward+ 均 0 failures。覆盖移除全部石斧后木剑准入、空手按 5 切木剑、滚轮第 5 槽回第 1 槽、M 开地图、经 GUI 鼠标事件增删旗标、未探索/边界拒绝、960×600 与 1600×1000 窗口尺寸下坐标对应。
- GUI 测试使用 Viewport.push_input(event, true)，注入已换算的 viewport 坐标，走真实 GUI 分发，没有直接调用地图的 _gui_input。未探索和上限 toast 的文字显示未单独自动断言，上限规则由原专项覆盖。
- progress、playthrough、village、forest、mansion 及 mansion --reloadcheck：本轮 headless 均 0 failures。原生仅新增补测本轮重跑；三项原生截图为接班者已有产物，本次目视复核，不冒称重新生成。
- 审核时曾误把已完成营地 fixture 用于从零起步的 playthrough；纠正参数后通过，该次错误运行不算游戏缺陷。首次 GUI 测试亦因将 viewport 坐标传给窗口坐标 API 而失败，改用正确的事件坐标空间后通过；没有修改产品代码来消除这些失败。
- 日志 /tmp/quality-book.log、quality-extra.log、quality-extra-native.log、quality-progress.log、quality-playthrough-correct.log、quality-village.log、quality-forest.log、quality-mansion.log、quality-reload.log。临时日志非长期证据库；可用测试重现。
- 测试隔离玩家存档；没有结束用户游戏，没有提交或发布此次工作区。

## 工作单 A：先完成交付收尾

优先级：先做，完成后才进入 B。负责人：下一位接班开发者。

1. 修正上述四处提示，显示“木剑或石斧”，战斗操作包含 5/3；检查欢迎、目标、路牌、准备不足、入内说明一致。
2. 修原 test_book_review 的测试前置条件，或明确引用本次 test_quality_review 作为独立证据。必须在断言前确认背包无石斧；键盘测试从另一槽开始。保留本次 GUI 增删及尺寸回归。
3. 更新 BOOK_COVERAGE、VALIDATION 和 HANDOVER：按功能分别记录“已验证”“仍简化”“待发布”。删去当前状态中的过期指令，不删除历史经过。
4. 运行受影响专项、原生文案检查、相关回归。用准确命令：playthrough 从零开始，仅 --integration；村庄/森林/府邸/书页专项使用 camp_v1 fixture；府邸重载依赖先生成的临时存档。
5. 更新补丁版本与启动器，检查 diff 不含参考书页、玩家存档、构建缓存。沿用既有公开仓库发布授权，确认账号和 remote 后提交发布；没有新的权限阻碍时不用重复索要确认。Release 继续说明提供源码，不冒称独立安装包。

验收：四处界面一致；两个错误前置条件已修正；新补测纳入记录；版本、提交、Release 状态对应真实结果。

## 工作单 B：工具耐久与备用工具（下一项新开发）

目标：玩家能看到工具磨损，在损坏前准备备用工具；损坏后有明确提示，切换备用工具可继续采集或战斗，保存退出不会刷新耐久。

依据：印刷第 9 页工具损坏与备用装备，第 16–17 页武器/工具用途。动工前重读对应 XHTML 和图片；BOOK_COVERAGE 的 09b 为主项，相关工具条目列依赖。书未给出的精确耐久数值写为教学参数，不自行声称原版数值。

范围：现有木镐、石斧、木剑。火把、空手不加耐久。保持现有模型识别，HUD 给当前工具耐久与低耐久提醒；附魔、维修台、完整九槽背包、护甲和新矿区留到后续。

实施要求：

1. 先确定库存模型：现有库存只记录数量，但同类工具可能有不同耐久。使用稳定工具实例或等价的独立耐久记录，避免所有同类工具共享一条耐久。明确当前装备对应哪一件。
2. 配方产出新工具为满耐久；只在有效采集/有效命中时按已定义规则扣除。挥空、菜单暂停、重复领取失败不能消耗；攻击按成功次数扣除，不能每帧扣。
3. 归零后仅损坏当前一件，提示原因和备用数量；有备用时给明确切换操作，无备用回空手并指向制作。不能因不恰当早期限制让新手失去再造木镐的途径。
4. 存档版本迁移：旧档同类工具数量全部保留，并初始化合理耐久；备份后一次迁移，反复加载不能复制工具或恢复已损耗耐久。不要覆盖真实玩家存档。
5. 验证两把同类工具不同耐久、有效使用扣除、无效动作不扣、归零仅移除一件、备用切换、制作满耐久、读档保留、旧档数量不丢、营地从零仍可完成。使用缩短的测试耐久来覆盖损坏，不让测试长时间磨工具。
6. 原生查看耐久提示在中文/不同窗口尺寸下是否遮挡准星、食物栏和快捷槽；走一遍“准备备用→采集/战斗→损坏→切换→保存重进”。

交付物：数据与迁移说明、实现、专项及必要回归、原生截图、书页覆盖更新、版本说明。报告精确到需求行，不写“第 9 页全部完成”。

完成 B 后才规划“石镐与铁矿获取→冶炼→铁工具/护甲”；府邸视觉与 AI 改进独立排期，第 32 页后场景继续暂停。
